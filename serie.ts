export class Serie {
    constructor (public id: number, public title: string, public canal: string, public temporadas: number, public dp: string, public linkinfo: string, public image: string, ) {}
    
}